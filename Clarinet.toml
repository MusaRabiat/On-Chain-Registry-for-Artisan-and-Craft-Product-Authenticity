[project]
name = 'On-Chain-Registry-for-Artisan-and-Craft-Product-Authenticity'
description = ''
authors = []
telemetry = false
cache_dir = '.\.cache'
requirements = []
[contracts.nft]
path = 'contracts/nft.clar'
clarity_version = 3
epoch = 'latest'

[contracts.registry]
path = 'contracts/registry.clar'
clarity_version = 3
epoch = '3.2'

[contracts.provenance]
path = 'contracts/provenance.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
